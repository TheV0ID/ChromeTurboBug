{
  "name": "assets",
  "version": "1.0.0",
  "description": "CSS and JS assets",
  "main": "index.js",
  "scripts": {
    "build": "esbuild app/app.js --bundle --outfile=server/scripts.js",
    "watch": "esbuild app/app.js --bundle --outfile=server/scripts.js --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.14.39"
  },
  "dependencies": {
    "@hotwired/turbo": "^7.3.0",
    "bootstrap": "^5.1.3",
    "express": "^4.19.2"
  }
}
